name: Validate

on:
  push:
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Run tests
        run: PYTHONPATH=. pytest

      - name: Creator calibration status
        run: PYTHONPATH=. python scripts/validate/check_calibrations.py

      - name: Validate Markdown
        run: python scripts/validate/validate_markdown.py

      - name: Build vector indexes
        run: |
          PYTHONPATH=. python scripts/index/build_vector_index.py
          PYTHONPATH=. python scripts/index/build_dense_index.py

      - name: Golden question retrieval eval
        run: PYTHONPATH=. python scripts/validate/run_golden_questions.py

      - name: Response quality eval
        run: PYTHONPATH=. python scripts/validate/run_response_eval.py
