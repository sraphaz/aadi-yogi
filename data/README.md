# Data

This tree stores non-canonical operational artifacts such as raw scans, OCR outputs, Markdown work products, chunks, and indexes.

Canonical spiritual source content belongs under `content/`, not `data/`.
