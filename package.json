{
  "name": "aadi-yogi",
  "private": true,
  "scripts": {
    "validate:content": "python scripts/validate/validate_markdown.py",
    "test": "PYTHONPATH=. pytest",
    "lint:python": "ruff check .",
    "pipeline:normalize": "python scripts/convert/normalize_md.py",
    "pipeline:chunk": "python scripts/chunk/semantic_chunk.py",
    "pipeline:embeddings": "python scripts/index/prepare_embeddings.py",
    "pipeline:index": "PYTHONPATH=. python scripts/index/build_vector_index.py",
    "eval:golden": "PYTHONPATH=. python scripts/validate/run_golden_questions.py",
    "api:dev": "PYTHONPATH=. uvicorn apps.agent-api.main:app --reload --host 0.0.0.0 --port 8000"
  }
}
