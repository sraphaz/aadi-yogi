# Processed Content

This tree is reserved for normalized, chunked, annotated, and embeddings-ready artifacts derived from curated sources.

It should never be confused with canonical source storage.
